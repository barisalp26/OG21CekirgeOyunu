using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Follow : MonoBehaviour
{
    //public GameObject karakter;
    //public GameObject darkPanel;
    //public GameObject follow;
    //Transform transform;
    //// Start is called before the first frame update
    //void Start()
    //{
    //}

    //// Update is called once per frame
    //void Update()
    //{
    //    Vector3 tempLocalScale = karakter.transform.localScale;
    //    transform.localScale.y.Equals(tempLocalScale.y);
    //    transform.localScale.x.Equals(tempLocalScale.x);
    //    transform.localScale.z.Equals(tempLocalScale.z);
    //}
}
